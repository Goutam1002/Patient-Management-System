# Implementation Progress — Patient Management App

**Branch/worktree:** `brd/review-report` (`.claude/worktrees/brd-review-report`) — `EnterWorktree` refused a nested worktree (session already inside one); creating a sibling worktree via `git worktree add` would have excluded uncommitted edits to `implementation-brd.md`/`CLAUDE.md` this work depends on. Building directly here instead, per `implementation-brd.md`'s documented fallback. Only implementation-work commits are made by this process — pre-existing uncommitted changes in this worktree are left untouched.
**Plan:** `docs/plan-brd-review.md`, Steps 1–6 (Phase 0: Scaffold; Phase 1: Data model & migrations) — session scope confirmed with the user.
**Dev database:** `(localdb)\MSSQLLocalDB` — SQL Server LocalDB, already registered on this machine. `docs/brainstorm-brd-review.md` §3.3 recommends SQL Server Express (named instance) for the doctor's actual deployment machine; LocalDB is used here for development only, connection string swappable later without a code change.
**Fixed spec version note:** built against `implementation-brd.md` as edited by the user this session — Angular **19**, .NET **10** Web API, Bootstrap 5.x for styling (pinned versions, not "latest").
**Session scope complete:** Steps 1–6 all `Done`, `dotnet test` 16/16 passing. Next up per `docs/plan-brd-review.md`: Phase 2 (Step 7, Visit-creation API) is the first step needing a genuinely new decision-free implementation — everything through Step 6 was schema/service-layer only, no HTTP surface yet.
**Restructured 2026-08-24 (user request):** both projects moved from top-level `frontend/`/`backend/` into a shared `src/` — now `src/frontend/` and `src/backend/`. The "Files" column in each step below still shows the paths as they were *at the time that step was built* (`frontend/...`, `backend/...`); read every such path as now prefixed with `src/`. `docs/plan-brd-review.md`'s file targets for Steps 7+ have been updated to the new `src/`-prefixed paths directly, so no translation is needed there. Verified after the move: `dotnet build`/`dotnet test` (16/16) from `src/backend/`, and `ng build` from `src/frontend/` all succeed unchanged — nothing in the code referenced the old top-level paths, so this was a pure filesystem move with no source changes.

| Step | Plan Phase | Status | Files | Tests | Commit | Notes |
|---|---|---|---|---|---|---|
| 1 | Scaffold Angular 19 + .NET 10 Web API + local SQL Server wiring | Done | `frontend/*` (Angular 19 workspace, Bootstrap 5 wired into `angular.json`, `src/environments/*`), `backend/PatientManagement.slnx`, `backend/PatientManagement.Api/*` (Program.cs, appsettings.json, `Data/AppDbContext.cs`, `Migrations/*_InitialCreate.cs`) | Manual: `dotnet build` (0 warnings after bumping `Microsoft.AspNetCore.OpenApi` to 10.0.11, which also cleared a NU1903 high-severity advisory on the template-default `Microsoft.OpenApi` 2.0.0); `ng build` (bundle budget raised 500kB→700kB to account for Bootstrap, still well under the 1MB error threshold); `dotnet ef database update` on the empty context succeeded against `(localdb)\MSSQLLocalDB` (created the `PatientManagement` DB + `__EFMigrationsHistory`); `dotnet run` confirmed Kestrel listens on `http://localhost:5212` and serves `/openapi/v1.json`. | `30a45fc` | Solution file is `.slnx` (new .NET 10 SDK default format), not the plan's assumed `.sln` — functionally equivalent, noted as a naming deviation. Removed template-default `WeatherForecast.cs`/`WeatherForecastController.cs`. Dev connection string uses `(localdb)\MSSQLLocalDB` per this progress file's header note (Windows-auth, no secret to manage). CORS policy reads the Angular origin from `AngularDevServerOrigin` config (`http://localhost:4200`). |
| 2 | `Users` entity + auth secret | Done | `Models/User.cs`, `Services/IPasswordCrypto.cs`, `Services/AesPasswordCrypto.cs`, `Data/DoctorAccountSeeder.cs`, `Data/AppDbContext.cs` (modified), `Program.cs` (modified), `appsettings.Development.json` (modified), `Migrations/*_AddUsers.cs`; new `PatientManagement.Api.Tests` xUnit project (added to solution) | `dotnet test`: 7/7 passed — 4 `AesPasswordCrypto` unit tests (round-trip, ciphertext≠plaintext, two encryptions of the same plaintext differ due to random IV, missing-key throws), 2 `DoctorAccountSeeder` tests (seeds once, idempotent on re-run) against EF Core InMemory, 1 migration smoke test applying all migrations to a fresh uniquely-named LocalDB database. Also manually verified end-to-end: ran the API, confirmed `Users` table has exactly `Id`/`Username`/`Password` columns (via `INFORMATION_SCHEMA.COLUMNS`), and the seeded row decrypts back to the configured plaintext. | `fbea283` | **Encryption key location:** `appsettings.Development.json` → `Auth:EncryptionKey`, a freshly generated random 256-bit key (`openssl rand -base64 32`), local-only per the accepted-risk rationale in `implementation-brd.md` (no hosting, no network exposure, one account — revisit if any of those three conditions changes). **Seed credentials:** username `doctor`, password `ChangeMe123!` (both overridable via `Auth:SeedDoctorUsername`/`Auth:SeedDoctorPassword` config) — confirmed with the user as a placeholder; there is no in-app way to change it later (no password-reset flow, by design), so a real deployment should override these before first run or update the row directly via SSMS. |
| 3 | `DoctorDetails` entity | Done | `Models/DoctorDetails.cs`, `Data/AppDbContext.cs` (modified), `Migrations/*_AddDoctorDetails.cs` | `dotnet test`: 7/7 passed — migration smoke test extended to assert `AddDoctorDetails` is applied. | `d485a3d` | `Logo`/`Signature` stored as `byte[]` (varbinary) columns directly on the row — this is a local-only, single-machine app with no external file/blob storage, so a byte[] column is simplest; not spelled out in the plan, noted here as the implementation choice. |
| 4 | `Patient` entity | Done | `Models/Patient.cs`, `Data/AppDbContext.cs` (modified), `Migrations/*_AddPatients.cs` | `dotnet test`: 10/10 passed — 3 new tests against a fresh LocalDB database: `PatientId` starts at 0 and increments (confirmed the `IDENTITY(0,1)` annotation actually took effect, not just present in the migration source), two patients can share one phone number without a constraint violation, `Age`/`DateOfBirth` persist independently. Migration smoke test extended for `AddPatients`. | `b3e9749` | **Age/DOB nullability:** the fixed spec requires both fields to exist and persist independently, but doesn't say both are mandatory at entry — read the BRD's "Age / DOB" as "capture whichever is known" and made both nullable rather than required. Documented here as the assumption rather than a stop-and-ask, since it's a data-entry default that's cheap to tighten later if wrong. `Gender` is a required free-text string (no closed enum invented — BRD doesn't specify allowed values). |
| 5 | `Appointment` + `Visit` entities | Done | `Models/AppointmentStatus.cs`, `Models/Appointment.cs`, `Models/Visit.cs`, `DTOs/WalkInVisitRequest.cs`, `Services/IWalkInService.cs`, `Services/WalkInService.cs`, `Services/AppointmentSlotConflictException.cs`, `Data/AppDbContext.cs` (modified), `Program.cs` (modified — registers `TimeProvider`/`IWalkInService`), `Migrations/*_AddAppointmentsAndVisits.cs` | `dotnet test`: 14/14 passed — 4 new `WalkInService` tests against fresh LocalDB databases: exactly one `Appointment` (Status=Completed) + one linked `Visit` created together, visit numbers increment per-patient not globally (two patients' first visits are both #1), a second appointment at an already-occupied instant is rejected (`AppointmentSlotConflictException`, using a fixed `TimeProvider` to force the collision deterministically), `Weight` round-trips at 3 decimal places. Migration smoke test extended. | `613c89c` | Vitals are C# non-nullable value types (`decimal`/`short`/`int`) — "cannot save a visit with missing vitals" is enforced by the type system itself, stronger than a runtime null-check; no separate test needed for that specific claim. Double booking enforced two ways: a unique DB index on `Appointment.ScheduledTime` (holds for any future insert path) plus a service-layer pre-check for a clean exception rather than a raw SQL constraint violation. `Visit.AppointmentId` is 1:1 (unique index) — an appointment produces at most one visit; not explicitly required by the plan but consistent with "a visit originates from a scheduled appointment." `CreateWalkInVisitAsync` takes vitals directly as parameters even though the HTTP endpoint that will call it doesn't exist until Step 7 — the fixed spec's own wording ("inserts the Appointment and Visit together") requires the full Visit row to be constructible now. |
| 6 | `Prescription` + `PrescriptionItem` entities | Done | `Models/Prescription.cs`, `Models/PrescriptionItem.cs`, `Data/AppDbContext.cs` (modified), `Migrations/*_AddPrescriptions.cs` | `dotnet test`: 16/16 passed — confirmed editing `DoctorDetails` after a `Prescription` is created does not retroactively change the prescription's already-snapshotted `ClinicName`/`DoctorName`/`RegistrationNumber` (the plan's own required test); confirmed `PrescriptionItem.DrugName` accepts arbitrary free text with no dictionary constraint. Migration smoke test extended. | — (uncommitted) | Snapshot behavior lives in `Prescription.CreateFromDoctorDetails(...)`, a static factory on the entity itself — the plan's file list didn't call for a separate service, and this is the only sanctioned construction path (a comment says so directly on the entity). Immutability itself (no update endpoint) is explicitly deferred to the controller layer per the plan — not built in this step, nothing to test yet. |

Status is one of: `Not started`, `In progress`, `Done`, `Blocked` (blocker named in Notes).
